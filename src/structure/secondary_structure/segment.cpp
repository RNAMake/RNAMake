//
// Created by Joe Yesselman on 6/27/22.
//

#include "segment.hpp"

namespace structure::secondary_structure {
  // Method Guidelines

  // This is for when we decide on a path forward, what exactly
  // the methods will do once implemented

  /*
  String get_string(structure::secondary_structure::Chain chain) {
    String s
    for (auto const &r : chain->_residues) {
      s += r.get_str() + ";";
    }
    return s
  }

  String get_string(structure::secondary_structure::Structure) {
    String s = "";
    for (auto const &chain : chains) {
      s += chain->get_string() + "|";
    }
  }
  */
}